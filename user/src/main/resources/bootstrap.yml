spring:
  application:
    name: api-user

  cloud:
    config:
      uri: http://api-config-server:8888

  profiles:
    active: prod

management:
  endpoints:
    web:
      exposure:
        include: '*'
