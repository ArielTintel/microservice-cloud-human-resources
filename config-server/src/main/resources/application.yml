spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ArielTintel/ms-cloud-configs
          username: ${GITHUB_USER}
          password: ${GITHUB_PASSWORD}

server:
  port: 8888